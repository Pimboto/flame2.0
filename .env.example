# Aplicación
NODE_ENV=development
PORT=3000

# Base de datos
DB_TYPE=sqlite
DB_DATABASE=database.sqlite

# CORS
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=info              # error, warn, info, debug, verbose
LOG_SQL=false              # true para ver todas las queries SQL
LOG_SQL_ERROR_ONLY=true    # true para ver solo errores SQL
